header.header
  .inner
    .logo
      | {{#link 'help_center' class='logo-link' }}
      img(src='{{logo_url}}', alt="{{t 'logo'}}")
      | {{/link}}
    .search-wrapper
      | {{search submit=false instant=true}}
    .nav-wrapper
      nav#user-nav.user-nav 
        a(href="https://dynamicsignal.desk.com/customer/portal/emails/new") Submit a Request
        | {{user_info}}  
  .alertbox
